import React from 'react';
import { Text,View } from 'react-native';
import {PersonalTab} from '../../nav/BtnRoute';
export default function LunTanPage() {
  return (
    <View style={{flex:1}}>
      <PersonalTab/>
    </View>
  );
}